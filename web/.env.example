# =============================================================================
# Next.js Frontend Environment Variables
# =============================================================================
# Copy this file to .env.local for local development
# In Sevalla, set these as environment variables in the dashboard

# Backend API URL
# Local development: http://localhost:8000
# Sevalla production: Use the internal connection URL from Sevalla dashboard
# Example: http://msl-api.internal:8000
BACKEND_URL=http://localhost:8000

# Public API URL (exposed to client-side, optional)
# Used if you need to call the backend directly from the browser
# In production, this should be your public backend URL or omitted if using proxy
NEXT_PUBLIC_API_URL=http://localhost:8000
